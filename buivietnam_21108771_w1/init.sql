CREATE TABLE IF NOT EXISTS books(
    id serial primary key,
    tensach text,
    giagoc text,
    giagiam text,
    tacgia text,
    nguoidich text,
    kichthuoc text,
    sotrang text,
    hinhthuc text
);
